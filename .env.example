# ==============================================================================
# DATABASE (Neon Postgres via Vercel)
# ==============================================================================
# When you add Neon Postgres to your Vercel project, these are auto-configured:
POSTGRES_PRISMA_URL="postgres://user:pass@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require"
POSTGRES_URL_NON_POOLING="postgres://user:pass@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require"

# For local development, you can use either:
# - The same Neon URLs above (connects to cloud database)
# - A local Postgres: DATABASE_URL="postgresql://user:pass@localhost:5432/calendar_assistant"

# ==============================================================================
# AUTHENTICATION
# ==============================================================================
# NextAuth URL (set to your production domain in Vercel)
NEXTAUTH_URL="http://localhost:3000"

# Auth secret - REQUIRED for production
# Generate with: openssl rand -base64 32
AUTH_SECRET="generate-a-secret-with-openssl-rand-base64-32"

# ==============================================================================
# GOOGLE OAUTH (from Google Cloud Console)
# ==============================================================================
# 1. Create a project at https://console.cloud.google.com
# 2. Enable Google Calendar API and Gmail API
# 3. Create OAuth 2.0 credentials (Web application)
# 4. Add authorized redirect URIs:
#    - http://localhost:3000/api/auth/callback/google (development)
#    - https://your-domain.vercel.app/api/auth/callback/google (production)
# Required OAuth scopes: calendar.readonly, calendar.events, gmail.send
GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-client-secret"

# ==============================================================================
# AI (Claude API)
# ==============================================================================
ANTHROPIC_API_KEY="sk-ant-..."
